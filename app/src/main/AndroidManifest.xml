<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.programmers.wine.gaslabs">

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".GasLabsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".ui.launch.LaunchActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.home.HomeActivity"
            android:label="@string/title_activity_home"
            android:parentActivityName=".ui.launch.LaunchActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.programmers.wine.gaslabs.ui.launch.LaunchActivity" />
        </activity>
        <activity
            android:name=".ui.about.AboutActivity"
            android:label="@string/title_activity_about"
            android:parentActivityName=".ui.home.HomeActivity"
            android:theme="@style/AppTheme.SwipeBack">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.programmers.wine.gaslabs.ui.home.HomeActivity" />
        </activity>

        <service
            android:name=".ui.bluetooth.connect.BluetoothLeService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".ui.bluetooth.DeviceActivity"
            android:label="@string/title_activity_device"
            android:parentActivityName=".ui.home.HomeActivity"
            android:theme="@style/AppTheme.SwipeBack">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.programmers.wine.gaslabs.ui.home.HomeActivity" />
        </activity>

        <service
            android:name=".ui.bluetooth.service.XiaomiService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>
